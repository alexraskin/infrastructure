# Proxmox API token lives here
terraform.tfvars
*.tfstate
*.tfstate.*
crash.log
.terraform*
!.terraform.lock.hcl

# k3s join token, age private key for SOPS
secrets/
age.key
*.plain.yaml

# cluster admin credentials
kubeconfig

# built images (GBs) and nix build symlinks
build/
result
result-*
*.qcow2
*.img

# leftovers from the old compose services still sitting on disk
.env
it-tools/
plex/
