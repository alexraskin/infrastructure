apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository
metadata:
  name: lhbotgo
  namespace: flux-system
spec:
  image: ghcr.io/alexraskin/lhbotgo
  interval: 72h
---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: lhbotgo
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: lhbotgo
  # The repo also carries `latest`, `vX`, `vX.Y` and per-commit tags. Only the
  # full release tags are candidates.
  filterTags:
    pattern: '^v?(?P<version>\d+\.\d+\.\d+)$'
    extract: '$version'
  policy:
    semver:
      range: '>=0.1.0'
